.worktrees/
.private-journal/
.claude/
.superpowers/
.DS_Store
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md / README.md). It is not part of the published
# plugin, so the whole directory is ignored here.
evals/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 안전관리 AI 비서 - 실제 현장 설정 (공사금액, 경로 등 비공개 정보)
apps/safety-assistant/site.yaml
apps/safety-assistant/site-data/
